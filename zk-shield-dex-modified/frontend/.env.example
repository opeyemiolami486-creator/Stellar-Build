# ZK Shield DEX — Frontend Environment Variables
# Copy to .env.local

# Backend API URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Stellar network (always testnet for this MVP)
NEXT_PUBLIC_NETWORK=testnet

/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_* vars in .env.local are automatically exposed to the browser.
  // Do NOT hard-code them here — that would override .env.local values.
  // See frontend/.env.example for the variables you need to set.
};

module.exports = nextConfig;
